package vault.voyage.app.panelFragments.todoFragment.recyclerview

import android.content.Context
import android.view.ViewGroup
import androidx.recyclerview.widget.RecyclerView
import vault.voyage.app.panelFragments.bagFragment.recyclerview.BagItem
import vault.voyage.app.panelFragments.bagFragment.recyclerview.BagItemsAdapter

class TodoItemsAdapter (val context: Context?, val items:List<TodoItem>): RecyclerView.Adapter<BagItemsAdapter.Holder>() {
    override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): BagItemsAdapter.Holder {
        TODO("Not yet implemented")
    }

    override fun getItemCount(): Int {
        TODO("Not yet implemented")
    }

    override fun onBindViewHolder(holder: BagItemsAdapter.Holder, position: Int) {
        TODO("Not yet implemented")
    }
}